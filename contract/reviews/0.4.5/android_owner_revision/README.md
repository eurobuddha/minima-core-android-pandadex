# Owner revision / cleanup gap — audit26

**771 Android assertions pass**:732 normal,14 before deliberate process death and25 after restart. Reuses audit25's complete isolated harness and adds22 assertions around actual SQLite owner revision, rollback, pending cleanup and export. Current production Java hashes match compilation/execution.

A saved edit remains pending after its owner archive was committed. SQLite triggers separately fail correction-archive insertion and owner-row update: both failures retain the old JSON and leave no partial correction. A new fixture history/onchain/linked-block reply is then processed by actual Pending.reconcile. Its full original source/edit effects and new inclusion are verified; exactly one pending receipt clears. Earlier bytes remain in one correction row, first observation is unchanged, no owner record or trade is added. An older inclusion is rejected. After the old winner is explicitly missing, a subsequently verified complete alternative succeeds and the streaming export includes both owner revisions without requiring a mytrade row.

Coverage qualification: the first rejected competing-candidate fixture also lacks block time, so that assertion alone does not isolate the missing-old-winner guard. ReceiptRepairTest covers that guard with complete proof at JVM level; the successful subsequent alternative exercises its integration. Add a complete pre-miss alternative integration fixture in the next owner-queue audit. The existing later process-death checks still test the original same-inclusion cleanup gap; this new moved-inclusion case executes within one process.

APK `/private/tmp/pandadex-android-audit-build-26/pandadex-export-audit-26.apk`, SHA-256 `2112c44e205a403b9083ecdfda7647d7b04101faad474be87d4e9d8ba19038fb`, package com.eurobuddha.pandadex.audit, versionCode26. Fresh output/number; earlier APKs preserved, production405 unbuilt. The first host runner stopped before install on a SHA256-field typo caused by audit-number substitution; corrected host script and note retained, no APK rebuild.

Fresh disposable Android16/API36 arm64 emulator with exact owned QEMU/AVD/path checks, isolated ADB5049. No INTERNET permission, production MainActivity/NodeTransportService, user phone/node, signing or submission. Transaction responses are fixtures. All emulator/isolated ADB/data/keys cleaned up. Raw logs/hashes retained here; audit screenshots and the earlier owner-access fixture ZIP remain in the temporary output directory. The new revision export is checked in the instrumentation, not that earlier retained ZIP. No fresh personal visual inspection claimed.
