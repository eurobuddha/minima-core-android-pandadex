<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ runtime notification permission for fill / renewal alerts -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Background order-watcher / GTC-renewal foreground service (M5) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Android 14+: a persistent chain-watcher uses the special-use FGS type (dataSync is capped at
         ~6h/day on Android 14 and crashes the service when exceeded) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Keep detecting fills / renewing GTC orders while the app is closed -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />

    <!-- Android 11+ package visibility: needed to launch the node app and to deliver
         our targeted (setPackage) IPC broadcasts to it. Same package name on the upstream
         Spartacus Rex node and the fork — this app targets the UPSTREAM node. -->
    <queries>
        <package android:name="org.minimarex.minimacore" />
    </queries>

    <!-- allowBackup=false is a FUND-SAFETY setting here, not a preference. With the scaffold default
         (true + empty rules) Android Auto Backup uploaded this app's whole data directory to the user's
         cloud and RESTORED it on reinstall — which both exfiltrates the data below and re-plants state a
         reinstall is supposed to clear. A reinstall must be a true reset.
         At stake: pandadex_keys → node_keys/node_addrs, the wallet ownership set that decides "is this order mine".
         Restored onto a different node it mis-answers in both directions: starving your own rungs, or filing
         a stranger's fills as yours. Live maker slots and pending orders would be re-planted too -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PandaDex"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".DexKeepAliveService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse|dataSync">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Watches the on-chain order book to detect fills and keep the user's GTC orders alive while the app is closed" />
        </service>

        <receiver android:name=".HeartbeatReceiver" android:exported="false" />

        <receiver android:name=".BootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
